#ifndef FILEUTILS_HPP
#define FILEUTILS_HPP

#include <string>

namespace config {

namespace fileutils {

bool isFile(const std::string& filepath);

} // namespace fileutils

} // namespace config

#endif
