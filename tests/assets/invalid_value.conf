root /var/www/html/;
max_body_size 4096;
keepalive_timeout 30a; #invalid - not a number

server {
    listen 8080;
    server_name main;
    max_body_size 4096;
    keepalive_timeout 10;
}

server {
    listen 9090;
    server_name backup;
    root /var/www/server2;
}
