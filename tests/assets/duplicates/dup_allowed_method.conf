root /var/www/html/;
max_body_size 512;
keepalive_timeout 10;

server {
    root /var/www/html/serv;
    listen 8080;
    server_name dup;

    location /images {
        root /pngs;
        allowed_methods GET GET;
    }
}