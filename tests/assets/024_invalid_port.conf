root /var/www/html/;
max_body_size 4096;
keepalive_timeout 30;

server {
    listen not_a_number; #invalid - not a number
    server_name main;
    max_body_size 4096;
    keepalive_timeout 10;
}

server {
    listen 9090;
    server_name backup;
    root /var/www/server2;
}
