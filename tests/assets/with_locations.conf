root /srv/www/;
max_body_size 2048;
keepalive_timeout 45;

server {
    listen 8080;
    server_name example.com;

    location /images/ {
        root /data/images/;
    }

    location /api/ {
        root /data/api/;
    }
}
