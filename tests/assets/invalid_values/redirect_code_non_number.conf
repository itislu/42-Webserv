root /var/www/;
max_body_size 1024;
keepalive_timeout 60;

server
{
  listen 8080;
  server_name serv1;
  
  location /redirect/ {
      redirect true;
      redirect_code abc;
      redirect_url http://example.com/;
  }
}
