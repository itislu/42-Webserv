root /var/www/;
max_body_size 1024;
keepalive_timeout 60;

server
{
  listen 8080;
  server_name serv1;
}

# to test allowed methods
server
{
  listen 8080;
  server_name methods;
  allowed_methods GET;
}
