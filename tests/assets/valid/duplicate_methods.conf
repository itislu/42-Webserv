root /var/www/;

server {
    listen 8080;
    server_name example.com example;

    location / {
        root /data/;
        allowed_methods GET POST POST;  # duplicate method
    }

    location /var {
        root /data/;
        allowed_methods GET GET;  # duplicate method
    }
}
